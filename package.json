{
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./assets/site.css --minify",
    "build": "npm run build:css && node scripts/build-static.mjs"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.19"
  }
}
